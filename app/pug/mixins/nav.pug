mixin navLink(link,title, frase)
	li&attributes(attributes)
		a(href=link)
			| #{title}
			br
			span
				| #{frase}
				
//mixin navLink(link,name)
	li
		a(href=link class={ active : name === current }) #{name}				